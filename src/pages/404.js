import React from 'react';

import Layout from '@common/Layout';
import { Container } from '@components/global';

const NotFoundPage = () => (
  <Layout>
    <Container>
      <h1>ARADIĞINIZ SAYFA BULUNAMADI</h1>
      <p>Geldiğin yoldan geri dön!</p>
    </Container>
  </Layout>
);

export default NotFoundPage;
